"""Get All SingleNeuronSimulation tool."""

from typing import ClassVar

from httpx import AsyncClient
from pydantic import Field

from neuroagent.tools.autogenerated_types.entitycore import (
    ListResponseSingleNeuronSimulationRead,
    ReadManySingleNeuronSimulationGetParametersQuery,
)
from neuroagent.tools.base_tool import BaseTool, EntitycoreMetadata


class SingleNeuronSimulationGetAllInput(
    ReadManySingleNeuronSimulationGetParametersQuery
):
    """Inputs for the single-neuron-simulation get all tool."""

    page_size: int = Field(
        ge=1,
        le=10,
        default=5,
        description="Number of items per page",
    )


class SingleNeuronSimulationGetAllTool(BaseTool):
    """Class defining the Get All Single Neuron Simulations logic."""

    name: ClassVar[str] = "entitycore-singleneuronsimulation-getall"
    name_frontend: ClassVar[str] = "Get All Single Neuron Simulations"
    description: ClassVar[
        str
    ] = """Searches a neuroscience based knowledge graph to retrieve single-neuron-simulations.
    The output is a list of single-neuron-simulations, containing:
    - The single neuron simulation ID and basic identifiers
    - Core single neuron simulation information and properties
    - Simulation parameters and configuration
    - Creation and modification timestamps
    """
    description_frontend: ClassVar[
        str
    ] = """Search and retrieve single-neuron-simulations. Use this tool to:
    • Find single-neuron-simulations by various criteria
    • Access detailed single-neuron-simulation data

    Specify optional criteria to find relevant single-neuron-simulations."""
    metadata: EntitycoreMetadata
    input_schema: SingleNeuronSimulationGetAllInput

    async def arun(self) -> ListResponseSingleNeuronSimulationRead:
        """Extract single-neuron-simulations.

        Returns
        -------
            list of single-neuron-simulations and their metadata, or an error dict.
        """
        query_params = self.input_schema.model_dump(exclude_defaults=True, mode="json")
        query_params["page_size"] = self.input_schema.page_size

        headers: dict[str, str] = {}
        if self.metadata.vlab_id is not None:
            headers["virtual-lab-id"] = self.metadata.vlab_id
        if self.metadata.project_id is not None:
            headers["project-id"] = self.metadata.project_id

        response = await self.metadata.httpx_client.get(
            url=self.metadata.entitycore_url.rstrip("/") + "/single-neuron-simulation",
            headers=headers,
            params=query_params,
        )
        if response.status_code != 200:
            raise ValueError(
                f"The single-neuron-simulation endpoint returned a non 200 response code. Error: {response.text}"
            )

        response_data = response.json()
        return ListResponseSingleNeuronSimulationRead(**response_data)

    @classmethod
    async def is_online(cls, *, httpx_client: AsyncClient, entitycore_url: str) -> bool:
        """Check if the tool is online."""
        response = await httpx_client.get(
            f"{entitycore_url.rstrip('/')}/health",
        )
        return response.status_code == 200

name: Autogen

on:
  pull_request:
  push:
    branches: [main]

jobs:
  autogen-checks:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout latest commit
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5  # Updated to v5
        with:
          python-version: "3.11"

      - name: Install datamodel-code-generator
        run: |
          pip install datamodel-code-generator[http]

      - name: Run autogen commands
        run: |
          # Define base URL and output directory
          BASE_URL="https://staging.openbraininstitute.org/api"
          OUTPUT_DIR="src/neuroagent/tools/autogenerated_types"

          # Array of services and their corresponding output files
          declare -A SERVICES=(
            ["entitycore"]="entitycore.py"
            ["obi-one"]="obione.py"
            ["literature"]="literature.py"
            ["bluenaas"]="bluenaas.py"
            ["thumbnail-generation"]="thumbnail_generation.py"
          )

          for service in "${!SERVICES[@]}"; do
              datamodel-codegen \
                --enum-field-as-literal=all \
                --target-python-version=3.11 \
                --reuse-model \
                --field-constraints \
                --input-file-type=openapi \
                --output-model-type=pydantic_v2.BaseModel \
                --openapi-scopes {schemas,paths,parameters} \
                --use-standard-collections \
                --use-union-operator \
                --use-default-kwarg \
                --use-operation-id-as-name \
                --extra-fields=forbid \
                --url="${BASE_URL}/${service}/openapi.json" \
                --output="${OUTPUT_DIR}/${SERVICES[$service]}"
            done

      - name: Check for meaningful changes
        run: |
          BASE_PATH="src/neuroagent/tools/autogenerated_types"

          # First, collect all files under BASE_PATH that have any changes
          ALL_CHANGED=$(git diff --name-only HEAD -- "$BASE_PATH")

          # Now filter to only those files where the diff (minus timestamp lines) is non-empty
          MEANINGFUL_CHANGED=()
          for file in $ALL_CHANGED; do
            # strip out timestamp-only diffs, then look for any remaining +/- lines
            if git diff HEAD -- "$file" \
                 | grep -v '^[+-]#   timestamp:' \
                 | grep -q -E '^[+-][^+-]'; then
              MEANINGFUL_CHANGED+=("$file")
            fi
          done

          if [ ${#MEANINGFUL_CHANGED[@]} -gt 0 ]; then
            echo "❌ Autogenerated files are outdated!"
            echo ""
            echo "Changed files:"
            for f in "${MEANINGFUL_CHANGED[@]}"; do
              echo "  - $f"
            done
            echo ""
            echo "Please run the autogen commands locally and commit the changes."
            exit 1
          else
            echo "✅ All autogenerated files are up to date"
          fi
